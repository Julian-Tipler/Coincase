import { connect } from 'react-redux';
import RecentTransactions from './recent_transactions'

const msp = (state) => ({

})

const mdp = dispatch => ({

})

export default connect(msp,mdp)(RecentTransactions)