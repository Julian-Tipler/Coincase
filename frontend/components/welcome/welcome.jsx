import { connect } from 'react-redux';
import WelcomeContainer from './welcome_container'

const msp = (state) => ({

})

const mdp = dispatch => ({

})

export default connect(msp,mdp)(WelcomeContainer)